# API 服务端隐式记忆验证实验

## 一、实验目的

本实验并非用于验证 LLM 模型本身是否具备记忆能力，而是用于验证：

> **在当前 LangChain + DeepSeek API 的调用方式下，不同 `model.invoke()` 请求之间是否存在由 API 服务端自动维护的对话记忆。**

这是后续研究 Agent Memory 的前置实验。

如果 API 服务端本身已经根据用户、API Key、会话 ID 等信息自动保存并恢复历史，那么后续自己实现的 Memory 实验就无法确定：

```
模型能够记住信息
```

究竟来自：

```
自己的 Memory
```

还是：

```
API 服务端隐藏的 Memory
```

因此首先需要尽可能排除服务端隐式保存历史的影响。

------

# 二、实验思路

实验分为两个阶段。

### 实验一：单次运行内的历史记忆

在程序内部维护：

```
history = []
```

每次调用：

```
model.invoke(messages)
```

之前，将历史消息显式加入：

```
messages = [
    system_message,
    *history,
    current_message,
]
```

这样可以验证：

> 当历史消息**明确提供给 LLM** 时，模型是否能够利用这些信息。

------

### 实验二：重新启动程序

关闭第一次运行的 Python 程序，然后重新启动程序。

此时：

```
history = []
```

重新初始化。

也就是说，第二次运行过程中：

```
第一次运行的 history
        ↓
     不存在
        ↓
第二次运行
```

如果 API 服务端仍然能够知道第一次运行中的对话内容，则说明服务端可能存在某种跨请求/跨会话的历史保存机制。

反之，如果第二次运行无法获得第一次运行的信息，则至少说明：

> **在本实验所采用的 API 调用方式和测试条件下，没有观察到服务端自动恢复第一次程序运行中的对话历史。**

------

# 三、实验代码

```
from langchain_deepseek import ChatDeepSeek
from langchain_core.messages import HumanMessage, AIMessage

model = ChatDeepSeek(
    model="deepseek-flash",
    temperature=0,
    max_tokens=None,
    timeout=None,
    max_retries=2,
    api_key="sk-",
)

history = []

while True:
    user_input = input("你：")

    if user_input.lower() in ["exit", "quit", "q"]:
        break

    messages = [
        ("system", "你是一个中文语音助手，请根据用户的输入返回相应的文本"),
        *history,
        ("human", user_input),
    ]

    ai_msg = model.invoke(messages)

    print("AI：", ai_msg.content)

    history.append(("human", user_input))
    history.append(("ai", ai_msg.content))
```

这里需要注意一个关键点：

```
history = []
```

是**应用程序自己的 Memory**。

而：

```
*history
```

则是把应用程序保存的历史显式重新发送给模型。

因此本实验中，模型能够记住上下文是**预期行为**，因为历史已经明确存在于请求中。

------

# 四、实验一：程序运行期间的显式 Memory

## 4.1 第一次输入

```
你：你好，我叫爱洗澡的猫
```

LLM 返回：

```
你好呀，爱洗澡的猫！很高兴认识你，有什么我可以帮你的吗？
```

程序随后保存：

```
Human:
你好，我叫爱洗澡的猫

AI:
你好呀，爱洗澡的猫！很高兴认识你，有什么我可以帮你的吗？
```

此时：

```
history
```

已经不再为空。

------

## 4.2 第二次输入

```
你：我是谁？
```

由于程序将 `history` 重新加入请求：

```
System
Human：你好，我叫爱洗澡的猫
AI：你好呀，爱洗澡的猫！很高兴认识你，有什么我可以帮你的吗？
Human：我是谁？
```

因此模型返回：

```
你叫“爱洗澡的猫”呀，是你刚刚告诉我的～😊
```

### 实验结果

模型能够利用第一次交互中的信息。

但这个结果**不能用于证明 API 服务端具有记忆**，因为历史是由程序主动提供的。

------

# 五、实验一继续：验证完整历史是否被传入

继续输入：

```
你：请直接返回上一次的输出
```

LLM 返回：

```
你叫“爱洗澡的猫”呀，是你刚刚告诉我的～😊
```

这进一步说明当前 `history` 中包含之前的：

```
Human：我是谁？

AI：你叫“爱洗澡的猫”呀，是你刚刚告诉我的～😊
```

并且模型能够根据这些上下文生成对应回答。

------

## 5.1 请求历史检查

继续输入：

```
你：你能把history输出吗
```

LLM 返回：

```
当前对话历史如下：

1. 用户：你好，我叫爱洗澡的猫
2. 助手：你好呀，爱洗澡的猫！很高兴认识你，有什么我可以帮你的吗？
3. 用户：我是谁？
4. 助手：你叫“爱洗澡的猫”呀，是你刚刚告诉我的～😊
5. 用户：请直接返回上一次的输出
6. 助手：你叫“爱洗澡的猫”呀，是你刚刚告诉我的～
7. 用户：你能把history输出吗
```

这与程序中的：

```
history
```

所保存的内容相符。

因此可以确认当前程序确实在执行：

```
用户输入
    ↓
LLM
    ↓
AI 输出
    ↓
history 保存
    ↓
下一次 invoke
    ↓
history + 新输入
    ↓
LLM
```

------

# 六、实验二：重新启动程序

结束第一次运行：

```
你：q
```

随后重新执行：

```
PS python .\TestLLMMem.py
```

此时程序重新执行：

```
history = []
```

因此第二次运行的第一次请求实际上只有：

```
System：
你是一个中文语音助手，请根据用户的输入返回相应的文本

Human：
你知道我是谁吗
```

注意：

**这里没有任何第一次程序运行中的 HumanMessage 或 AIMessage。**

------

## 6.1 实验结果

输入：

```
你：你知道我是谁吗
```

LLM 返回：

```
我不知道你是谁哦，因为我没法识别你的个人身份，也不会访问你的个人信息。如果你愿意，可以告诉我你想让我怎么称呼你。
```

此时模型没有提到：

```
爱洗澡的猫
```

也没有恢复第一次运行中的：

```
你好，我叫爱洗澡的猫
```

等历史信息。

------

# 七、实验结果对比

| 测试场景               | 程序 `history` | 第一次运行历史是否发送 | 是否获得历史信息 |
| ---------------------- | -------------- | ---------------------- | ---------------- |
| 第一次运行，第一次请求 | 空             | 否                     | 否               |
| 第一次运行，后续请求   | 有             | 是                     | 是               |
| 第二次重新启动程序     | 空             | 否                     | 否               |

核心对比可以简化为：

```
第一次运行

history
   │
   ├── Human
   ├── AI
   ├── Human
   └── AI
        │
        ▼
   model.invoke()
        │
        ▼
   能够利用历史
```

而重新启动程序后：

```
第二次运行

history = []
        │
        ▼
   model.invoke()
        │
        ▼
   没有第一次运行历史
        │
        ▼
   无法恢复之前的信息
```

------

# 八、实验结论

本实验得到的结论应该比之前更加严谨：

> **在当前测试环境和 API 调用方式下，没有观察到 DeepSeek API 服务端在独立程序运行之间自动恢复此前对话历史的行为。**

也就是说，当前实验可以暂时排除这样一种情况：

```
第一次 invoke()
      ↓
DeepSeek 服务端偷偷保存历史
      ↓
程序退出
      ↓
第二次启动
      ↓
第二次 invoke()
      ↓
服务端自动把第一次历史注入
```

实际观察结果更符合：

```
第一次运行

Python
  │
  ├── history
  │
  ▼
model.invoke()
  │
  ▼
DeepSeek API
  │
  ▼
LLM
```

程序结束后：

```
history
   ↓
随着 Python 进程结束而消失
```

第二次运行：

```
新的 Python 进程
       │
       ├── history = []
       │
       ▼
model.invoke()
       │
       ▼
DeepSeek API
       │
       ▼
LLM
```

没有观察到第一次运行的历史被自动恢复。